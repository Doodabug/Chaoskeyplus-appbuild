{
  "name": "ChaosKeyPlus Token Integration",
  "symbol": "CKP",
  "decimals": 18,
  "standard": "STRK20",
  "version": "1.0",
  "description": "Submission manifest for STRK20 integration review. This file documents the STRK20 contract metadata and the expected interface for integration. No proprietary implementation details are included.",
  "interfaces": {
    "transfer": {
      "signature": "transfer(address to, uint256 amount) returns (bool)",
      "note": "Standard token transfer function"
    },
    "approve": {
      "signature": "approve(address spender, uint256 amount) returns (bool)",
      "note": "Standard approval function"
    },
    "transferFrom": {
      "signature": "transferFrom(address from, address to, uint256 amount) returns (bool)",
      "note": "Standard transferFrom function"
    },
    "balanceOf": {
      "signature": "balanceOf(address owner) view returns (uint256)",
      "note": "Query token balance"
    }
  },
  "deployment": {
    "network": "TO_BE_FILLED",
    "contract_address": "TO_BE_FILLED",
    "deployer_notes": "Use multisig-controlled deployer. Do not publish private keys in this repo."
  },
  "contact": {
    "maintainer": "doodabug",
    "email": "REDACTED"
  }
}
